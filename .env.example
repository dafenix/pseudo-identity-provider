# Pseudo Identity Provider Configuration

# Service Port (Host Port Mapping)
PSEUDO_IDP_PORT=8083

# Login Credentials
LOG_USERNAME=admin
# Default password is 'test'
# Generate new hash with: cd src/hash && go run hash_salt.go
# Note: Quote the password hash to prevent variable expansion
LOG_PASSWORD='$2a$10$s/jpYKi4n7NnKbpw/JKIX.GSVVId3lla6FbbnkECAMUnVh1azvnYm'

# Nginx Reverse Proxy Ports (when using --profile https)
HTTPS_PORT=9443
HTTP_PORT=8000

# Configuration File (optional)
# Uncomment to load config from file on startup
# CONFIG_FILE=/app/config/config.json
